node_modules/
dist/
out/
build/
.remotion/
*.log
.DS_Store
.env
.env.local
coverage/
.vite/
.turbo/
# The whole directory, not one path inside it. The narrow rule let a `remotion render`
# into `.scratch/` show up as an 8 MB untracked file that `git add -A` would have taken.
.scratch/
